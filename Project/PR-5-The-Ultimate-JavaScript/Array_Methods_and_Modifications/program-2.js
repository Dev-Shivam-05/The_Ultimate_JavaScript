// Q2. Write a program to remove the last element from an array using pop().

let array = [1,12,31,4,50,61,7,8,9];

console.log(`Current Array is : ${array}`);

let poped_element = array.pop(); // popping the last element from the array
console.log(`After Popping the last element ${poped_element} from the array it looks like :`);
console.log(array);